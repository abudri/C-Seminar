/* a header file we use for function declarations.  we define (with body) these functions 
wherever else we want in the code and order of appearance in the code won't matter, pg 147*/

float add_with_tax(float f);  /* a Declaration. Fuction will be Defined with body in the
 code that uses this header file.  This will be referenced in other files with #include "totaller.h" */
 // assd